# 「年年」语音拟人化改造分析（v2）

> 目标：让年年（球球）在聊天模式里像人一样——会停顿、会思考、会主动发起聊天。
> v2 修订：按反馈调整——**「主动发起聊天」是核心策略**（不是追问，追问只是它的一种形式）；**「停顿」确认走提示词+TTS 为主**，管道层只补句间间隙。

---

## 一、现状盘点

现有管道：`麦克风 → AEC(浏览器) → VAD(前端Silero+后端Silero) → ASR(阿里qwen3-asr) → LLM(DeepSeek流式) → TTS(阿里qwen3-tts-instruct-flash-realtime) → 扬声器`

| 已有能力 | 现状 | 对拟人化的价值 |
|---|---|---|
| 人格 prompt | `personality.py`：年年撒娇/傲娇/委屈/犯困 | 内容层"像猫"，缺对话行为规则 |
| 情绪→语气指令 | `voice_style.py`：7种情绪→自然语言TTS指令 | 语音层已有情绪底色，可加节奏/停顿描述 |
| 流式逐句 | LLM 按标点切句，一句生成即送 TTS | 停顿注入的天然插入点（句子间隙） |
| 思考状态 | `state=thinking` 只是 UI 状态，无声音反馈 | 可升级为"思考声"占位 |
| 打断 | 双层 VAD + barge-in，被打断回「嗯？」 | 已有"被打断反应" |
| 短期记忆 | `session.history`（最后20条） | 主动聊天的话题基础 |
| **无** | 句间停顿、思考音频、主动开口调度 | 本次要补的三块 |

---

## 二、拟人化拆解：三层金字塔

```
        认知层（像不像"活的"）      记忆连续、会主动开口、有情绪状态
        ─────────────────────────
        行为层（像不像"在对话"）    句间停顿、思考声、插话反应、延迟塑造
        ─────────────────────────
        语音层（像不像"在说话"）    语气、断句、气口、语气词、尾音
```

反直觉的关键点：**人不是匀速机器**。句与句之间、回复之间的停顿是**可变且有语义的**。现在的"零停顿匀速输出"就是机械感的根源。

---

## 三、停顿：一半靠提示词，一半靠管道

### 3.1 你的判断对不对？—— 对一半

**✅ 对的部分：句子内部的停顿，只靠提示词 + TTS 就能做到，不用改代码**
- **文本层**：`voice_style.py` 的 VOICE_GUIDE 里要求年年"用短句、多用 `……` 和 `——`"，LLM 就会产出带停顿感的文本；
- **TTS 层**：`EMOTION_INSTRUCTIONS` 从"语气"扩展成"语气+节奏+停顿"，例如委屈 = "慢一点说，句与句之间多停顿，像要哭又忍住"。qwen3-tts-instruct 吃自然语言指令，逗号、省略号、破折号它自己会渲染出韵律停顿。

**❌ 不对的部分：句与句之间的停顿，TTS 管不到，必须管道层补**
- 现在 `llm.chat_stream` 按 `。！？；…\n` 切句，**每一句是独立合成**的；
- 每句合成互不相干，"句与句之间停多久"完全由管道决定——而现在是**零间隔**（`await tts.speak_and_send` 完立刻下一句），所以听感是连珠炮；
- 修法：`handle_user_speech` 句子间隙插 `asyncio.sleep(150~500ms 随机)`，按句末标点和情绪加权（问句后拉长、委屈翻倍）。**必须可打断**（sleep 期间收到打断要能退出）。

**⚠️ 还有一个隐藏问题**：切句标点里包含 `…`，会把"嗯……让我想想"从中间劈成两句，省略号的停顿感直接被切没了。修法：**把 `…` 从 `SENTENCE_ENDS` 里去掉**，让省略号留在句内，由 TTS 自己渲染停顿。

### 3.2 改动清单

| 层 | 改什么 | 要不要动代码 |
|---|---|---|
| 文本层 | VOICE_GUIDE：短句、多用 `……`/`——`、偶尔带"嗯…" | 只改 prompt ✅ |
| TTS层 | EMOTION_INSTRUCTIONS：加"节奏/停顿"描述 | 只改配置 ✅ |
| 管道层 | ① `…` 移出切句标点；② 句间插随机 sleep | 各 1~2 行代码 |

---

## 四、思考：用"思考声"把延迟翻译成思考感

- LLM 首字延迟 ~1s，现在是干等（UI 转圈），听感像"机器卡住"；
- 设计：**预合成并缓存** 2~3 条短占位音频（"嗯…让我想想喵~"、"唔…这个嘛…"），启动时合成好存内存，播放零延迟；
- 阈值控制：首 token >0.8s 才播（快的不播，避免啰嗦）；播放期间用户开口 → 走现有 thinking 分支，可被打断；
- 进阶：让 LLM 输出 `<think>…</think>` 段，把推理时长映射成占位声长短。

---

## 五、主动发起聊天策略（核心）

> 追问不是独立能力，是主动聊天的一种形式（延续层/关心层里的问句）。
> 统一策略：**规则定时机 → LLM 定内容 → 反馈调频率**。

### 5.1 设计原则

1. **时机是策略，内容是生成**：用规则决定"何时说"（可控、可解释、好调参），用 LLM 决定"说什么"（自然、带记忆）；
2. **边界感优先**：主动开口最大的风险是烦人。频率、时机、话题都要克制，宁可少，不可烦；
3. **反馈闭环**：用户回应得多的话题多说，不回的少说/不说——主动开口要"越聊越懂"而不是"越聊越烦"；
4. **不打扰**：球球正在播报、用户正忙（环境有活动声但没说话）、周围安静没人时不开口（对着空气说话很吓人）。

### 5.2 何时说（WHEN）—— 触发规则

| 触发源 | 条件 | 示例 |
|---|---|---|
| **空闲** | 回复播完 + 用户沉默 8~15s（随机） | "刚才那个事……你真的要去呀？" |
| **时间** | 问候窗口（早 8-9 / 午 12-13 / 晚 22-23） | "主人早呀，今天有什么安排？" |
| **记忆事件** | 用户提过"明天要面试/考试/开会"，到点前主动问 | "明天要面试啦，准备得怎么样？" |
| **情绪感知** | 用户情绪低落，沉默中主动关心 | "感觉你今天不太开心……要不要说给我听？" |
| **冷场延续** | 对话自然结束后 ~2 分钟，回扣上一话题 | "对了，你刚才说的那个……后来怎么样了？" |

### 5.3 何时不说（WHEN NOT）—— 边界层

- 每会话主动次数上限（**2~3 次**）；
- 最小间隔（**≥3 分钟**）；
- 连续 2 次主动开口用户都没回应 → **本会话不再主动**（"用户不想聊"信号）；
- 球球正在播 / 用户正在说 → 不触发；
- 深夜（23:00~7:00）只触发睡眠关怀，不触发闲聊；
- 环境能量低（麦克风安静、无人声）→ 不触发（可能没人）。

### 5.4 说什么（WHAT）—— 内容层

话题分层（按优先级）：
1. **记忆层（最高）**：读长期记忆里"用户近期事件/偏好"→ 让 LLM 据此生成。只有记得的人才会这么问，最有"人味"；
2. **时间层**：时间窗口问候；
3. **延续层**：回扣上一话题；
4. **关心层**：健康提醒（喝水/睡觉）——要克制，像妈妈一样唠叨是减分项；
5. **卖萌层（最低频）**：随机萌语，一天最多 1~2 次。

生成方式：给 LLM 一个主动场景 prompt：
> "现在是年年主动找主人说话，根据以下记忆和上下文，说一句自然的话。要求：一到两句、口语化、最好带一个轻问题让对话能继续，但不要问需要长篇回答的问题。"

### 5.5 说完之后（AFTER）—— 反馈闭环

- 主动话播完 → 正常 listening，恢复空闲计时；
- 用户回应了 → 正常对话继续，该话题**响应 +1**；
- 用户沉默 → 不重复同一话题，该话题**响应 -1**；
- 用户打断 → 走现有 barge-in（"啊？"），说明时机不对，频控计数。

### 5.6 架构落点（怎么改代码）

```python
# ① ConversationSession 新增字段
session.proactive_count = 0      # 本会话主动次数
session.last_proactive_ts = 0    # 上次主动时间
session.topic_scores = {}        # 话题 → 响应分（反馈闭环）
session.idle_task = None         # 空闲计时器

# ② client_playback_done 进入 listening 时：启动空闲计时器
session.idle_task = asyncio.create_task(wait_and_maybe_proactive(ws, session))

# ③ 收到 speech_start / 音频帧时：取消计时器（用户开口=取消）
if session.idle_task and not session.idle_task.done():
    session.idle_task.cancel()

# ④ 计时器本体
async def wait_and_maybe_proactive(ws, session):
    await asyncio.sleep(random.uniform(8, 15))   # 空闲窗口随机
    if not _should_proactive(session):           # 频控/时段/在场检查
        return
    line = await llm.proactive_line(session)     # 带记忆上下文的生成
    await tts.speak_and_send(ws, line, session.session_id)
    session.proactive_count += 1
    session.last_proactive_ts = time.time()
    # 播完回 listening，等待用户回应或下一轮空闲
```

- 长期记忆：`backend/data/memory.json` 存 `{最近事件, 偏好, 话题响应统计}`；
- `_should_proactive()` = 次数上限 + 最小间隔 + 时段 + 环境能量，全部通过才开口。

---

## 六、底层支撑

### 6.1 情绪状态机持久化
现在每轮独立选情绪，会"一秒委屈一秒开心"。改成 session 维护 `mood` + 衰减规则（情绪随时间回归平静），每轮加权融合再送 TTS。

### 6.2 长期记忆
主动聊天的弹药库。`backend/data/` 已有目录，加 JSON/SQLite 存 `{用户名字, 偏好, 近期事件, 话题响应统计}`。

### 6.3 延迟塑造
简单闲聊快回，复杂问题慢回（思考占位+长停顿）。**延迟曲线随内容难度变化，听感立刻像人**。

---

## 七、落地优先级（建议）

| 阶段 | 内容 | 难度 | 拟人收益 |
|---|---|---|---|
| P1 | 停顿：prompt+TTS指令改（几乎零成本） | ★ | 句子内部立刻有节奏 |
| P1 | 停顿：`…`移出切句 + 句间随机 sleep | ★ | 句与句有气口 |
| P1 | 思考占位音频（预合成+阈值播放） | ★★ | 把延迟变思考感 |
| P2 | 主动开口调度器 v1（空闲计时+频控+时间触发） | ★★★ | 从"应答机"变"活物" |
| P2 | 长期记忆（memory.json + 响应统计） | ★★★ | 主动开口的话题弹药库 |
| P3 | 主动开口进阶（记忆事件触发+情绪感知触发） | ★★★ | "越聊越懂" |

> P1 全部是"改提示词 + 几行代码"，一两天内出效果；P2 的调度器是架构级新增，但只动 `main.py` 一个文件加少量字段，不动既有打断/流式链路。

---

## 八、一句话总结

- **停顿**：句子内部靠提示词+TTS（零代码），句与句之间管道补几行 sleep；
- **思考**：预合成"嗯…"占位声，把 LLM 延迟翻译成思考过程；
- **主动聊天**：规则定时机（WHEN）、LLM 定内容（WHAT）、反馈调频率（AFTER）——克制而聪明，让年年自己开口，且越聊越懂你。
