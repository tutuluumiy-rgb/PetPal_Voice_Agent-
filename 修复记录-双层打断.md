# 双层打断架构改造记录

## 改造目标
解决「打断不稳定、吞字、延迟大」的问题。采用前后端双层独立判断架构。

## 核心设计
```
前端（体感层，快）：只上报事件 + 本地 ducking，不做业务决策
  - Silero VAD 检测到人声 → 本地 ducking（压到20%）+ 上报 speech_start
  - 人声结束 → 上报 speech_end
  - 误报 → 恢复音量，不上报

后端（业务层，准）：状态机决策，判断人声/噪声
  - 收到 speech_start，基于当前状态决策：
    state=speaking → 真打断：停LLM/TTS + 缓存音频回放ASR
    state=thinking → 暂不打断，等 speech_end 确认
    state=listening → 正常说话，启动ASR
  - 收到 speech_end → 触发 finalize 识别
```

## 关键改动
### 前端（frontend/index.html）
1. 顶部新增参数区：DUCKING_GAIN=0.2、DUCKING_TIMEOUT_MS=800、VAD阈值等
2. VAD 回调改为纯事件上报（speech_start/speech_end），不再判断 ballIsPlaying 触发打断
3. 新增 startDucking/stopDucking（gain 平滑压到20%，超时恢复）
4. 新增 barge_confirm/barge_reject 消息处理（后端决策结果）

### 后端（backend/main.py）
1. handle_audio_frame 简化：只负责分发（speaking缓存 / listening喂ASR），端点检测交给前端事件
2. 新增 handle_speech_start：状态机决策（speaking→打断 / thinking→暂缓 / listening→正常）
3. 新增 handle_speech_end：触发 finalize
4. speaking/thinking 期间音频缓存（speaking_audio_cache，2秒），打断时回放ASR

## 参数位置
- 前端参数：frontend/index.html 顶部「🎛 前端打断参数区」
- 后端参数：main.py 顶部「🎛 端点检测参数区」

## 待验证
1. 球球说话时插话 → 立即 ducking → 后端确认真打断 → 完全静音
2. 误报（咳嗽）→ ducking 后 800ms 自动恢复音量
3. 正常说话 → 完整识别，不吞字
4. 连续多次打断稳定性

## 回滚
改造前版本：git 提交 2ebb00d（首次提交）
```bash
git diff 2ebb00d -- backend/main.py frontend/index.html
```
