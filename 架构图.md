# AI 宠物「年年」语音管道架构图

## 三阶段总览

```mermaid
flowchart TB
    subgraph FE[前端浏览器 - 体感层]
        MIC[麦克风采集]
        AEC[WebRTC AEC 回声消除]
        VADF[Silero VAD 前端检测<br>敏感阈值 0.4]
        RING[环形缓存 2秒<br>回退预卷256ms]
        DUCK[ducking 压音量<br>DUCKING_GAIN=0.1]
        PLAY[Web Audio 播放器<br>gainNode + 时间线调度]
        EVENT[事件上报<br>speech_start/speech_end]
    end

    subgraph BE[后端 FastAPI - 业务层]
        WS[WebSocket<br>/ws/audio]
        STATE[状态机<br>listening/thinking/speaking]
        VADB[Silero VAD 二次确认<br>严格阈值 0.45]
        ASR[Qwen3-ASR 流式识别]
        LLM[DeepSeek 流式生成<br>逐句切分]
        TTS[Qwen3-TTS 流式合成<br>逐句发送]
        CACHE[球球说话音频缓存<br>最多2秒]
        HISTORY[对话历史 + 打断标记]
    end

    MIC --> AEC --> VADF
    AEC --> RING
    VADF --> DUCK
    VADF --> EVENT
    RING --> EVENT

    EVENT -- "speech_start+预卷" --> WS
    EVENT -- "speech_end" --> WS

    WS --> STATE
    STATE --> VADB
    STATE --> ASR
    STATE --> CACHE
    VADB --> CACHE
    ASR --> LLM
    LLM --> TTS
    LLM --> HISTORY
    TTS -- "音频块 320ms/片" --> WS
    WS -- "barge_confirm/barge_reject" --> DUCK
    WS -- "TTS音频块" --> PLAY
    WS -- "client_playback_done" --> STATE
```

## 阶段一：开始说话（用户主动发起对话）

```mermaid
sequenceDiagram
    participant M as 麦克风
    participant A as AEC(浏览器)
    participant V as 前端Silero VAD
    participant R as 环形缓存
    participant F as 前端事件
    participant D as Ducking
    participant B as 后端状态机
    participant S as ASR
    participant L as LLM
    participant T as TTS
    participant P as 播放器

    Note over M,P: 【开始说话】球球未在播放，用户主动开口
    M->>A: 采集音频
    A->>A: 消回声(参考信号=播放内容)
    A->>V: 处理后的音频
    A->>R: 写入环形缓存(持续滚动2秒)
    A->>F: 实时发后端(ws.send)

    V->>V: 连续3帧人声概率>0.4
    V->>D: onSpeechStart触发
    D->>D: 球球没在播放 → 跳过ducking
    V->>R: 切出预卷256ms
    V->>F: 上报 speech_start + preRollBase64
    F->>B: WebSocket发送

    B->>B: state=listening → 正常说话分支
    B->>S: 启动ASR流式会话
    Note over F,S: 实时音频持续喂ASR

    V->>F: onSpeechEnd(人声结束)
    F->>B: 上报 speech_end
    B->>S: finalize → 识别完整句子
    S-->>B: 返回文本
    B->>L: 送LLM生成
    L->>L: 流式生成+情绪标签解析
    L-->>B: 逐句yield
    B->>T: 每句合成TTS
    T-->>P: 音频块(320ms/片)经WebSocket
    P->>P: 时间线调度播放
```

## 阶段二：插话打断（球球正在播放时用户开口）

```mermaid
sequenceDiagram
    participant M as 麦克风
    participant A as AEC(浏览器)
    participant V as 前端Silero VAD
    participant R as 环形缓存
    participant D as Ducking
    participant F as 前端事件
    participant B as 后端状态机
    participant C as 后端音频缓存
    participant VB as 后端Silero二次确认
    participant T as TTS
    participant L as LLM
    participant S as ASR
    participant P as 播放器

    Note over M,P: 【插话】球球正在播放时用户开口
    P->>P: 球球正在播(TTS音频块)
    M->>A: 采集音频(含球球回声+用户插话)
    A->>A: 消回声(但会削弱用户插话)
    A->>V: 处理后的音频
    A->>R: 写入环形缓存
    A->>F: 实时发后端

    V->>V: 检测到人声
    V->>D: onSpeechStart
    D->>D: 球球在播 → ducking生效<br>gain压到0.1
    V->>R: 切预卷256ms
    V->>F: 上报 speech_start + preRollBase64
    F->>B: WebSocket发送

    B->>B: state=speaking → 打断分支
    B->>C: 球球说话期间缓存的音频
    B->>C: 合并前端预卷256ms
    B->>VB: 二次确认(取最近256ms窗口)
    VB->>VB: Silero判断人声/噪声
    alt 判定人声
        VB-->>B: 确认打断
        B-->>F: barge_confirm
        F->>D: 取消ducking(已无意义)
        F->>P: stopStreamPlayback<br>立即停止播放
        B->>T: tts.cancel + abort_speaking
        B->>L: 停止LLM流水线
        B->>S: 喂预卷+缓存 → 启动新ASR
        B->>B: state=listening
        B->>B: history加打断标记
    else 判定噪声/回声
        VB-->>B: 拒绝打断
        B-->>F: barge_reject
        F->>D: stopDucking 恢复音量
        B->>B: 球球继续播放
    end
```

## 阶段三：连续对话（多轮循环）

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant B as 后端
    participant S as ASR
    participant L as LLM
    participant T as TTS
    participant P as 播放器

    Note over U,P: 【连续对话】多轮循环
    U->>F: 开口说话
    F->>B: speech_start(正常说话分支)
    F->>S: 喂实时音频
    U->>F: 说完
    F->>B: speech_end
    B->>S: 识别完整句
    B->>L: 生成回复
    L-->>B: 逐句
    B->>T: 合成TTS
    T-->>P: 音频块
    P->>P: 播放所有块

    P->>B: client_playback_done(真正播完)
    B->>B: state=speaking → listening

    Note over U,P: 用户再次开口 → 回到阶段一
```

## 关键机制汇总

| 机制 | 位置 | 参数/阈值 | 作用 |
|------|------|-----------|------|
| AEC | 前端浏览器 | getUserMedia echoCancellation:true | 消回声(参考信号=播放内容) |
| 前端VAD | 前端 | 阈值0.4, 3帧确认 | 快速检测人声 |
| 环形缓存 | 前端 | 2秒滚动, 回退256ms | 补VAD延迟丢的首字 |
| Ducking | 前端 | DUCKING_GAIN=0.1 | 插话瞬间压音量(听觉反馈) |
| 后端VAD | 后端 | 阈值0.45, 人声占比0.05 | 二次确认人声/噪声 |
| ASR | 后端 | Qwen3-ASR-Flash-Realtime | 流式识别 |
| LLM | 后端 | DeepSeek 流式 | 生成+情绪标签 |
| TTS | 后端 | Qwen3-TTS-Instruct-Flash | 流式合成, 320ms/片 |
| 播放器 | 前端 | Web Audio 时间线 | 顺序播放音频块 |

## 状态机

```mermaid
stateDiagram-v2
    [*] --> listening: 初始化
    listening --> listening: 用户说话(speech_start正常)<br>启动ASR
    listening --> thinking: speech_end<br>ASR识别
    thinking --> speaking: LLM生成第一句
    speaking --> speaking: TTS逐句播放
    speaking --> listening: 插话确认(barge_confirm)<br>停止播放+新ASR
    speaking --> speaking: 二次确认拒绝(barge_reject)<br>球球继续
    listening --> [*]: 连接关闭
```
