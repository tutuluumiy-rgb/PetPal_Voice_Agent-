# 打断修复记录

## 问题现象
打断后球球没有立刻停止，而是「等下一段内容展现后才说话」，甚至延续之前的话题（比如继续数数）。

## 根因分析
1. **cancel_event 语义被破坏**：我把 `tts_engine.py` 的 `synth_stream` 开头的 `self.cancel_event.clear()` 删掉了，想在打断后阻止所有后续合成。但这改变了 cancel_event 的语义（从「当前句级」变成「全局级」），与首次提交（稳定版）不一致。
2. **打断后的历史标记**：打断后 LLM 不知道「上一轮被打断」，会基于旧历史继续之前话题。

## 修复内容
1. **`backend/tts_engine.py`**：恢复 `synth_stream` 开头的 `self.cancel_event.clear()`（回到首次提交逻辑）
2. **`backend/main.py`**：去掉 `handle_user_speech` 里我加的 `tts.cancel_event.clear()`（与首次提交一致）
3. **`frontend/index.html`**：去掉 `stopStreamPlayback` 里加的调试 console.log（恢复干净）

## 保留的增强（不涉及回滚）
- **ASR 流式展示**：`asr_partial` / `asr_final` 消息（新增功能，不影响打断）
- **打断后 history 标记**：`client_barge_in` 里追加「主人打断了上一轮回复」标记，让 LLM 不延续旧话题
- **instruct 语气升级**：voice_style.py 拆分 + Mochi 音色

## 回滚方法
如果仍不稳定，可用 git 对比当前工作区与首次提交 `2ebb00d`：
```bash
git diff 2ebb00d -- backend/tts_engine.py backend/main.py frontend/index.html
```
需要完全回滚打断相关：恢复 `tts_engine.py` 和 `main.py` 的打断逻辑为首次提交版本。

## 验证
重启后端 + 刷新前端，测试：
1. 球球说话时打断 → 应立即停止
2. 打断后说新的话 → 球球回应新内容，不延续旧话题
