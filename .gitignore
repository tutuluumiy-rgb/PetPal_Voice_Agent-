# ── 环境变量（含 API key，绝对不能提交）──
.env
*.env
.env.*

# ── Python 缓存 ──
__pycache__/
*.pyc
*.pyo

# ── 模型文件（体积大，可重新下载）──
backend/models/
frontend/vad/*.wasm
frontend/vad/*.onnx

# ── 临时文件 ──
test.mp3
Untitled

# ── 日志 ──
*.log
tmp/
