# AI 宠物「球球」改造计划

> 目标：从「小智硬件 Demo」升级为「网页语音 Agent Demo」，实现能中途插话、能干活、且高度拟人化的 AI 宠物。
> 定位：车载语音助理架构的可展示映射，服务于汽车行业 AI 语音岗位面试。

---

## 一、为什么这么改（背景结论）

### 1.1 硬件方案为什么放弃

小智面包板是**单工 I2S** 架构（`AUDIO_I2S_METHOD_SIMPLEX`），麦克风链路拿不到扬声器参考信号，导致：

- 设备端 AEC 物理上不可用（无参考通道）
- 服务端 AEC 标注 Unstable，实测仍被本地 VAD 回声误触发，反复自打断

结论：**实时插话（barge-in）在该硬件上做不到，是硬件架构决定的，不是软件问题。**

### 1.2 为什么换网页

浏览器内置 WebRTC AEC3 回声消除，`getUserMedia({ echoCancellation: true })` 即可让浏览器拿「自己正在播放的音频」当参考信号，从麦克风里减掉。无需硬件参考通道，就能做到接近 GPT Live 的中途插话。

### 1.3 架构选择：级联模拟端到端（方案二）

端到端模型（GPT Live / Gemini Live / Qwen3-Omni）擅长「保留声学信息」，但上下文短、工具调用弱、记忆差，不适合「能干活的宠物」。

正确分层：**耳朵（流式 ASR）→ 大脑（文字 LLM + 工具 + 记忆）→ 嗓子（情感 TTS）**，用工程手段模拟端到端的体验。

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│  浏览器前端（网页 Demo）                              │
│  ├── 录音：getUserMedia + WebRTC AEC（消回声）       │
│  ├── 本地 VAD：检测用户是否开口 → 触发打断           │
│  ├── 播放：Web Audio API 播放 TTS 音频流             │
│  └── 表情：Canvas/CSS 渲染球球表情                   │
└──────────────┬──────────────────────────────────────┘
               │ WebSocket（音频流 + 文本事件 + 情绪标签）
┌──────────────▼──────────────────────────────────────┐
│  轻量后端 FastAPI                                    │
│  ├── 流式 ASR：sherpa-onnx + SenseVoice（本地免费）  │
│  ├── 大脑：DeepSeek + function calling + 记忆        │
│  ├── 情感 TTS：EdgeTTS（免费）→ 可升级情感模型        │
│  ├── 打断管理：barge-in 状态机                       │
│  └── 拟人化引擎：思考停顿 / 语气词 / 情绪状态机       │
└──────────────┬──────────────────────────────────────┘
               │ OpenAI 兼容 API
┌──────────────▼──────────────────────────────────────┐
│  外部服务：DeepSeek（LLM）· EdgeTTS（语音）· 天气等  │
└─────────────────────────────────────────────────────┘
```

---

## 三、关键模块设计

### 3.1 浏览器端（前端）

| 组件 | 方案 | 说明 |
|------|------|------|
| 回声消除 | `getUserMedia({ echoCancellation: true, noiseSuppression: true })` | WebRTC AEC3，消回声的关键 |
| 打断检测 | 本地 VAD（能量阈值 + 或集成 WebRTC VAD） | 检测用户开口 → 发打断信号 |
| 音频上传 | WebSocket 二进制帧，16kHz PCM | 流式上传，不等待说完 |
| 音频播放 | Web Audio API 流式播放 | 边收边播，降低首字延迟 |
| 球球表情 | CSS 动画 + 情绪状态 | 开心/困/委屈/好奇切换 |

### 3.2 流式 ASR（耳朵）

- **首选**：sherpa-onnx + SenseVoice（本地免费、低延迟、中文好）
- **备选**：阿里云 paraformer-realtime-v2（云端、效果更稳）
- 输出两态：**partial**（中间结果，用于快速响应）+ **final**（最终结果，用于提交 LLM）

### 3.3 大脑（LLM + Agent）

- DeepSeek + function calling
- 工具集：查天气、设置提醒、联网搜索、记便签
- 记忆：短期（对话历史）+ 长期（用户姓名、偏好，用 JSON 文件或 SQLite）
- **输出格式：让 LLM 在回复里带情绪标签**，如 `[开心]今天见到主人好高兴喵~`

### 3.4 情感 TTS（嗓子）

- 起步：EdgeTTS（免费、Xiaoxiao 音色）
- 升级：火山引擎情感 TTS / Hume AI / GLM-TTS（支持情绪参数）
- 情绪标签 → TTS 参数映射（语速、音调、情感）

### 3.5 拟人化引擎（核心差异化）

| 技巧 | 实现 | 效果 |
|------|------|------|
| 思考停顿 | LLM 推理期间播放「嗯…让我想想喵」占位音频 | 把延迟变成思考感 |
| 语气词注入 | prompt 引导 + 后处理插入「嗯/啊/咦」 | 不像机器匀速输出 |
| 语速节奏 | TTS 语速参数随情绪/内容变化 | 重要内容慢，闲聊快 |
| 情绪状态机 | 持久情绪（开心/困/委屈/好奇）+ 触发规则 | 情绪影响语音风格 |
| 打断回应 | 被打断时先「嗯？」再听新指令 | 自然的插话体验 |

### 3.6 打断管理（barge-in 状态机）

```
Listening ──用户开口──> 打断信号 ──> 停止 TTS 流 ──> 清队列 ──> 开始听新输入
```

- **硬打断**：立即停播 + 播放「嗯？」表示在听
- **半打断**（可选进阶）：先降低音量，确认用户真在说话再停

---

## 四、里程碑

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| M1 跑通级联 | 第 1 周 | 网页录音 + 回放 + ASR→LLM→TTS 完整对话 |
| M2 打断 + 拟人化 | 第 2 周 | 中途插话 + 思考停顿 + 语气词 |
| M3 Agent 化 | 第 3 周 | 工具调用 + 记忆 + 能干活的宠物 |
| M4 情绪 + 打磨 | 第 4 周 | 情绪状态机 + 面试演示 + 材料 |

---

## 五、面试价值映射

| 汽车语音考察点 | 本项目对应 |
|----------------|-----------|
| 回声消除 AEC | WebRTC AEC3 的原理与应用 |
| 中途打断 barge-in | 打断状态机 + VAD 阈值调优 |
| 流式 ASR 低延迟 | 流式语音管线设计 |
| 语音 Agent 工具调用 | 宠物干活 = 车载语音控车同构 |
| 情感化语音 | 情绪状态机 + 情感 TTS |
| 端到端 vs 级联权衡 | 「为什么不选端到端当大脑」的技术决策 |
